# setup

1. tar -xzf data.tar.gz  
2. install following packages:
    - torch
    - numpy
    - tqdm
3. install local packages (in packages/)

for sparsechem:
unzip packages/utils/sparsechem.zip -d packages/utils/
pip install -e packages/utils/sparsechem/

# experiments
follow the instructions from src/main.py

# docs
check it for more detailed info regarding code